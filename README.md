# Attendance

## Notes
- Excel exports are handled through the Azure Logic App workflow; the legacy `excel-export-code.js` script has been removed.

## GitHub Pages
This repository is ready for GitHub Pages as a static site. Deploy the root of the repository and ensure GitHub Pages is configured to serve from the main branch (root). A `.nojekyll` file is included to disable Jekyll processing. 
